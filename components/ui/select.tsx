"use client"; import * as React from "react"; import { cn } from "@/lib/utils"; interface SelectContextType { value: string; onValueChange: (val: string) => void; open: boolean; setOpen: (open: boolean) => void; name?: string; } const SelectContext = React.createContext<SelectContextType | null>(null); export function Select({ children, name, defaultValue = "", value: controlledValue, onValueChange }: { children: React.ReactNode; name?: string; defaultValue?: string; value?: string; onValueChange?: (val: string) => void; }) { const [val, setVal] = React.useState(defaultValue); const [open, setOpen] = React.useState(false); const currentVal = controlledValue !== undefined ? controlledValue : val; const handleSelect = (v: string) => { setVal(v); onValueChange?.(v); setOpen(false); }; return (<SelectContext.Provider value={{ value: currentVal, onValueChange: handleSelect, open, setOpen, name }}><div className="relative inline-block w-full">{children}</div></SelectContext.Provider>); } export const SelectTrigger = React.forwardRef<HTMLButtonElement, React.ButtonHTMLAttributes<HTMLButtonElement>>(({ className, children, ...props }, ref) => { const ctx = React.useContext(SelectContext); return (<button type="button" ref={ref} onClick={() => ctx?.setOpen(!ctx.open)} className={cn("flex w-full items-center justify-between text-left cursor-pointer", className)} {...props}>{children}<svg className={`w-4 h-4 ml-2 transition-transform duration-200 ${ctx?.open ? "rotate-180" : ""}`} fill="none" stroke="currentColor" viewBox="0 0 24 24"><path strokeLinecap="round" strokeLinejoin="round" strokeWidth={2} d="M19 9l-7 7-7-7" /></svg></button>); }); SelectTrigger.displayName = "SelectTrigger"; export function SelectValue({ placeholder }: { placeholder?: string }) { const ctx = React.useContext(SelectContext); return <span>{ctx?.value || placeholder || "Select"}</span>; } export function SelectContent({ children, className }: { children: React.ReactNode; className?: string; }) { const ctx = React.useContext(SelectContext); if (!ctx?.open) return null; return (<div className={cn("absolute z-50 mt-1.5 w-full rounded-lg bg-neutral-900 border border-white/15 p-1 shadow-2xl text-white backdrop-blur-xl overflow-hidden", className)}>{children}</div>); } export function SelectItem({ value, children, className }: { value: string; children: React.ReactNode; className?: string; }) { const ctx = React.useContext(SelectContext); const isSelected = ctx?.value === value; return (<div onClick={() => ctx?.onValueChange(value)} className={cn("cursor-pointer rounded-md px-3 py-2 text-sm transition-colors hover:bg-white/15 flex items-center justify-between", isSelected ? "bg-white/20 text-white font-medium" : "text-white/80", className)}>{children}{isSelected && (<svg className="w-4 h-4 text-white" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path strokeLinecap="round" strokeLinejoin="round" strokeWidth={2} d="M5 13l4 4L19 7" /></svg>)}</div>); }
